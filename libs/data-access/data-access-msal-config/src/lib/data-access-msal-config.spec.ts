import { dataAccessMsalConfig } from './data-access-msal-config';

describe('dataAccessMsalConfig', () => {
  it('should work', () => {
    expect(dataAccessMsalConfig()).toEqual('data-access-msal-config');
  });
});
